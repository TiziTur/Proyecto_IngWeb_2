{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules 2>/dev/null; npm ci --legacy-peer-deps && npm run build"
  },
  "deploy": {
    "startCommand": "node dist/main.js",
    "healthcheckTimeout": 120
  }
}
