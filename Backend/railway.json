{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules package-lock.json 2>/dev/null; npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node dist/main.js",
    "healthcheckTimeout": 120
  }
}
